import 'dart:typed_data';

Future<void> saveKorlixGeneratedImage({
  required Uint8List bytes,
  required String filename,
  required String mimeType,
}) async {
  throw UnsupportedError('Saving generated images is not supported here.');
}
